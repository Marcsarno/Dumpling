import fs from 'node:fs';import {Quat,Vec3,Mat4} from 'playcanvas';
const b=fs.readFileSync('artifacts/outdoors/source/kay/KayKit_Character_Animations_1.1/Animations/gltf/Rig_Medium/Rig_Medium_Tools.glb'),j=JSON.parse(b.subarray(20,20+b.readUInt32LE(12))),bin=b.subarray(28+b.readUInt32LE(12));
const acc=i=>{const a=j.accessors[i],v=j.bufferViews[a.bufferView],n={SCALAR:1,VEC3:3,VEC4:4}[a.type],r=[];for(let k=0;k<a.count;k++)for(let c=0;c<n;c++)r.push(bin.readFloatLE((v.byteOffset??0)+(a.byteOffset??0)+k*(v.byteStride??n*4)+c*4));return r;};
const parents={};j.nodes.forEach((n,i)=>n.children?.forEach(c=>parents[c]=i));
function worlds(local){const cache={};function w(i){if(cache[i])return cache[i];const n=local[i],m=new Mat4().setTRS(new Vec3(...n.p),new Quat(...n.q),new Vec3(...n.s));return cache[i]=parents[i]===undefined?m:new Mat4().mul2(w(parents[i]),m);}return j.nodes.map((_,i)=>w(i));}
const rest=j.nodes.map(n=>({p:n.translation??[0,0,0],q:n.rotation??[0,0,0,1],s:n.scale??[1,1,1]}));const names=['hips','spine','chest','head','upperarm.l','lowerarm.l','wrist.l','upperarm.r','lowerarm.r','wrist.r'],ids=names.map(n=>j.nodes.findIndex(x=>x.name===n));
const quats=ws=>ids.map(i=>{const q=new Quat().setFromMat4(ws[i]);return [q.x,q.y,q.z,q.w].map(v=>+v.toFixed(6));});const result={source:'KayKit Character Animations 1.1, CC0',bones:names,rest:quats(worlds(rest)),clips:{}};
for(const a of j.animations.filter(a=>['Fishing_Cast','Fishing_Idle','Fishing_Bite','Fishing_Reeling','Fishing_Catch'].includes(a.name))){const channels=a.channels.map(c=>{const s=a.samplers[c.sampler];return {node:c.target.node,path:c.target.path,t:acc(s.input),v:acc(s.output)};}),duration=Math.max(...channels.map(c=>c.t.at(-1))),frames=[],positions=[];for(let f=0;f<=Math.ceil(duration*30);f++){const t=Math.min(duration,f/30),local=structuredClone(rest);for(const c of channels){let i=0;while(i<c.t.length-2&&c.t[i+1]<t)i++;const n=c.path==='rotation'?4:3,u=Math.min(1,Math.max(0,(t-c.t[i])/(c.t[i+1]-c.t[i]||1)));let v;if(n===4){const q=new Quat().slerp(new Quat(...c.v.slice(i*n,i*n+n)),new Quat(...c.v.slice((i+1)*n,(i+2)*n)),u);v=[q.x,q.y,q.z,q.w];}else v=c.v.slice(i*n,i*n+n).map((v,k)=>v+(c.v[(i+1)*n+k]-v)*u);local[c.node][c.path==='rotation'?'q':c.path==='translation'?'p':'s']=v;}const w=worlds(local);frames.push(quats(w));positions.push(ids.map(i=>w[i].getTranslation().toArray().map(v=>+v.toFixed(6))));}result.clips[a.name]={duration,fps:30,frames,positions};}
fs.mkdirSync('public/assets/outdoors',{recursive:true});fs.writeFileSync('public/assets/outdoors/fishing-motion.json',JSON.stringify(result));console.log(Object.entries(result.clips).map(([n,c])=>[n,c.duration,c.frames.length]));
