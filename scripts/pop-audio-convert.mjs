import {writeFile} from 'node:fs/promises';
const {chromium}=await import('file:///C:/Users/marc7/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs');
const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage();await page.goto('http://127.0.0.1:5173/scripts/pop-core.html',{waitUntil:'domcontentloaded'});
for(const name of ['click_001','drop_001','drop_002','drop_003','pluck_001','confirmation_001']){const result=await page.evaluate(async name=>{const ctx=new AudioContext(),buffer=await ctx.decodeAudioData(await (await fetch('/assets/pop/audio/'+name+'.ogg')).arrayBuffer());const data=Array.from(buffer.getChannelData(0)),rate=buffer.sampleRate;await ctx.close();return {data,rate};},name);const data=Buffer.alloc(44+result.data.length*2);data.write('RIFF');data.writeUInt32LE(data.length-8,4);data.write('WAVEfmt ',8);data.writeUInt32LE(16,16);data.writeUInt16LE(1,20);data.writeUInt16LE(1,22);data.writeUInt32LE(result.rate,24);data.writeUInt32LE(result.rate*2,28);data.writeUInt16LE(2,32);data.writeUInt16LE(16,34);data.write('data',36);data.writeUInt32LE(result.data.length*2,40);result.data.forEach((v,i)=>data.writeInt16LE(Math.round(Math.max(-1,Math.min(1,v))*32767),44+i*2));await writeFile('public/assets/pop/audio/'+name+'.wav',data);}await browser.close();console.log('Six CC0 effects converted to mono PCM WAV for Safari compatibility.');
