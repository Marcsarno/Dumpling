import assert from 'node:assert/strict';
import {chromium} from 'file:///C:/Users/marc7/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
const browser=await chromium.launch({channel:'msedge',headless:true}),page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),errors=[];page.on('pageerror',e=>errors.push(e.message));const snap=()=>page.evaluate(()=>window.__roomTest.snapshot());
try{await page.goto(process.env.GAME_URL??'http://127.0.0.1:5177');await page.waitForFunction(()=>window.__roomTest?.snapshot().dog);assert.ok(Math.abs((await snap()).dog.height-.48)<.0001,JSON.stringify((await snap()).dog));await page.locator('.dev-launch').tap();let a=(await snap()).dog;await page.waitForTimeout(1300);assert.deepEqual((await snap()).dog,a);await page.locator('.dev-close').tap();await page.waitForTimeout(500);assert.notEqual((await snap()).dog.time,a.time);await page.locator('#squishy-pop-shortcut').tap();await page.locator('[data-level="0"]').waitFor();a=(await snap()).dog;await page.waitForTimeout(1000);assert.deepEqual((await snap()).dog,a);await page.locator('#squishy-pop [data-back]').tap();assert.deepEqual(errors,[]);console.log('PASS measured 0.48-unit bind height; DEV and arcade pause freeze dog, resume restores animation');}finally{await browser.close();}
